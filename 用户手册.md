# StaffDeck 启动手册

## 1. 快速启动

项目目录：

```text
E:\TLong\StaffDeck
```

日常使用只需要以下命令：

```powershell
Set-Location E:\TLong\StaffDeck

# 后台启动
.\scripts\dev_up.ps1 --detach

# 查看状态和实际端口
.\scripts\dev_status.ps1

# 查看日志
Get-Content .dev\logs\app.log -Tail 100

# 停止
.\scripts\dev_down.ps1
```

常用页面与验证地址：

| 功能 | 地址 |
| --- | --- |
| 数字员工广场 | `http://127.0.0.1:5173/workspace/gallery` |
| 聊天页面 | `http://127.0.0.1:5173/workspace/chat` |
| 管理后台 | `http://127.0.0.1:5173/enterprise/dashboard` |
| 健康检查 | `http://127.0.0.1:5173/api/health` |

默认端口为 `5173`。如果它被占用，脚本会自动从 `5173–5199` 中选择端口；用下面命令查看实际端口并验证服务：

```powershell
.\scripts\dev_status.ps1
Get-Content .dev\app.port
curl.exe http://127.0.0.1:5173/api/health
```

默认管理员账号：

```text
用户名：admin
密码：admin
```

## 2. 第一次安装

只需执行一次：

```powershell
Set-Location E:\TLong\StaffDeck

# 后端环境
py -3 -m venv backend\.venv
.\backend\.venv\Scripts\python.exe -m pip install -e "backend[dev]"

# 前端环境
npm --prefix frontend-enterprise ci

# 创建配置文件
Copy-Item backend\.env.example backend\.env
```

环境要求：Python 3.11+、Node.js 20+、npm。

如果 `backend\.env` 已存在，不要再次复制，避免覆盖配置。

## 3. 必要配置

配置文件：

```text
E:\TLong\StaffDeck\backend\.env
```

主要修改以下内容：

```dotenv
APP_SECRET="请替换为长随机字符串"

DATABASE_URL="sqlite:///./skill_agent_loop.db"

DEMO_MODEL_BASE_URL="https://模型服务地址/v1"
DEMO_MODEL_NAME="模型名称"
DEMO_MODEL_API_KEY="API Key"

TOOL_BASE_URL="http://localhost:5173"
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"
```

说明：

| 配置 | 作用 |
| --- | --- |
| `APP_SECRET` | 登录令牌和敏感数据使用的密钥，必须修改 |
| `DATABASE_URL` | 数据库地址 |
| `DEMO_MODEL_BASE_URL` | 大模型 API 地址 |
| `DEMO_MODEL_NAME` | 模型名称 |
| `DEMO_MODEL_API_KEY` | 模型 API Key |

首次启动后，也可以登录管理后台修改模型配置。

## 4. 数据库和日志

默认数据库配置：

```dotenv
DATABASE_URL="sqlite:///./skill_agent_loop.db"
```

对应文件：

```text
E:\TLong\StaffDeck\backend\skill_agent_loop.db
```

如需修改 SQLite 文件位置：

```dotenv
DATABASE_URL="sqlite:///E:/StaffDeckData/staffdeck.db"
```

当前项目主要支持 SQLite，不建议直接切换到 MySQL 或 PostgreSQL。

常用文件位置：

| 内容 | 路径 |
| --- | --- |
| 数据库 | `backend/skill_agent_loop.db` |
| 应用日志 | `.dev/logs/app.log` |
| 启动日志 | `.dev/logs/supervisor.log` |
| 启动错误 | `.dev/logs/supervisor.err.log` |
| 当前端口 | `.dev/app.port` |

持续查看日志：

```powershell
Get-Content .dev\logs\app.log -Wait
```

## 5. 前后端分开启动（仅开发调试）

后端使用 `8000`，前端使用 `5173`。

第一个 PowerShell 启动后端：

```powershell
Set-Location E:\TLong\StaffDeck\backend
$env:CORS_ORIGINS = "http://localhost:5173,http://127.0.0.1:5173"
$env:TOOL_BASE_URL = "http://127.0.0.1:8000"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

第二个 PowerShell 启动前端：

```powershell
Set-Location E:\TLong\StaffDeck
$env:VITE_PROXY_TARGET = "http://127.0.0.1:8000"
npm --prefix frontend-enterprise run dev
```

前端页面与后端验证接口：

```text
前端页面：http://127.0.0.1:5173/workspace/gallery
管理后台：http://127.0.0.1:5173/enterprise/dashboard
后端验证接口：http://127.0.0.1:8000/api/health
```

`8000` 仅提供后端 API，不提供管理页面；打开管理页面请使用 `5173`。

## 6. 常见问题

### 页面能打开，但没有模型回复

检查 `backend/.env` 中的模型地址、模型名和 API Key，再查看：

```powershell
Get-Content .dev\logs\app.log -Tail 100
```

### 修改前端后没有变化

默认模式使用构建后的前端，重启即可重新构建：

```powershell
.\scripts\dev_down.ps1
.\scripts\dev_up.ps1 --detach
```

### `/docs` 无法打开

当前代码关闭了 Swagger。请使用 `/api/health` 检查服务。
