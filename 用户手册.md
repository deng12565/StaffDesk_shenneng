# StaffDeck 快速启动说明

项目目录：`E:\TLong\StaffDeck`

## 1. 日常启动

在 PowerShell 中执行：

```powershell
Set-Location E:\TLong\StaffDeck

# 后台启动
.\scripts\dev_up.ps1 --detach

# 查看运行状态、端口和健康检查
.\scripts\dev_status.ps1

# 停止
.\scripts\dev_down.ps1
```

默认地址：`http://127.0.0.1:5173`

如果 `5173` 被占用，启动脚本会从 `5173-5199` 中自动选择端口。实际端口以 `dev_status.ps1` 或 `.dev\app.port` 为准。

默认管理员账号：`admin` / `admin`。首次登录后应修改密码。

## 2. 常用页面

| 功能 | 地址 |
| --- | --- |
| 员工工具管理 | `http://127.0.0.1:5173/enterprise/tools` |
| 用户前端（数字员工广场） | `http://127.0.0.1:5173/workspace/gallery` |

## 3. 常用配置

配置文件：`backend\.env`

```dotenv
APP_SECRET="请替换为长随机字符串"

DATABASE_URL="sqlite:///./skill_agent_loop.db"

DEMO_MODEL_BASE_URL="https://模型服务地址/v1"
DEMO_MODEL_NAME="模型名称"
DEMO_MODEL_API_KEY="模型 API Key"

OPENET_API_TOKEN="OpenET API Token"

TOOL_BASE_URL="http://localhost:5173"
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"
```

修改模型、数据库地址或服务配置后，重启 StaffDeck。不要把真实 API Key 或 Token 写入源码、MCP 表单、日志或说明文档。

## 4. 数据库

### 默认数据库

项目使用 SQLite，默认配置为：

```dotenv
DATABASE_URL="sqlite:///./skill_agent_loop.db"
```

开发环境对应文件：`backend\skill_agent_loop.db`

应用启动时会自动启用 WAL 模式和 30 秒数据库忙等待。当前渠道功能要求使用文件型 SQLite，不要直接改成 MySQL、PostgreSQL 或内存数据库。

### 修改数据库位置

建议使用绝对路径，并使用正斜杠：

```dotenv
DATABASE_URL="sqlite:///E:/StaffDeckData/staffdeck.db"
```

修改前先停止服务，并确保目标目录已经存在。修改后重新启动，应用会在新数据库中自动建表；原数据库数据不会自动迁移。

### 查看数据库文件

```powershell
Get-Item .\backend\skill_agent_loop.db |
  Select-Object FullName, Length, LastWriteTime
```

### 备份数据库

SQLite 使用 WAL 模式，备份前先停止 StaffDeck，避免只复制主文件而遗漏尚未归并的数据。

```powershell
Set-Location E:\TLong\StaffDeck
.\scripts\dev_down.ps1

$backupDir = "E:\StaffDeckBackup"
$stamp = Get-Date -Format "yyyyMMdd-HHmmss"
New-Item -ItemType Directory -Force $backupDir | Out-Null
Copy-Item .\backend\skill_agent_loop.db "$backupDir\skill_agent_loop-$stamp.db"
```

备份完成后重新启动：

```powershell
.\scripts\dev_up.ps1 --detach
```

### 恢复数据库

恢复会覆盖当前数据。先停止服务，并把备份文件名替换为实际文件：

```powershell
Set-Location E:\TLong\StaffDeck
.\scripts\dev_down.ps1

Copy-Item "E:\StaffDeckBackup\skill_agent_loop-YYYYMMDD-HHMMSS.db" `
  ".\backend\skill_agent_loop.db" -Force

.\scripts\dev_up.ps1 --detach
```

不要单独编辑或复制运行中的 `.db-wal`、`.db-shm` 文件，也不要随意删除数据库重建；模型配置、MCP Server 和员工绑定等数据都保存在数据库中。

## 5. 本地 MCP

### 新建本地 MCP Server

1. 打开“员工工具管理”：`http://127.0.0.1:5173/enterprise/tools`。
2. 点击“新增 → 新建空白工具”，将工具类型切换为“MCP 服务器”。
3. 连接方式选择“Stdio（本地命令）”，填写 Command、Args、Env JSON 和工作目录。
4. 保存后点击“发现工具”，确认 MCP 返回的工具列表。
5. 需要给员工使用时，只勾选该员工职责所需的工具，再点击“导入/同步”。

本地 `stdio` MCP 不需要单独启动服务或开放端口。StaffDeck 会在发现或调用时启动子进程，并在本次 MCP 会话结束后关闭。

### OpenET 配置示例

| 字段 | 值 |
| --- | --- |
| 名称 | `openet` |
| 展示名称 | `OpenET 气象数据` |
| 连接方式 | `Stdio（本地命令）` |
| Command | `E:\TLong\StaffDeck\backend\.venv\Scripts\python.exe` |
| Args | 第一行 `-m`，第二行 `app.tools.openet_mcp` |
| Env JSON | `{}` |
| 工作目录 | `E:\TLong\StaffDeck\backend` |

OpenET Token 只配置在 `backend\.env`：

```dotenv
OPENET_API_TOKEN="OpenET API Token"
```

不要把 Token 填入 MCP 的 Env JSON、命令行参数或其他表单。

### 展示、发现和绑定

- MCP Server 保存后会显示在员工工具管理页，即使尚未绑定任何员工。
- `0 个工具`表示当前员工尚未绑定该 MCP 下的工具，不代表 MCP 不可用。
- “发现工具”只执行 `tools/list`，不会创建 Tool，也不会授予员工调用权限。
- “导入/同步”会创建或更新 StaffDeck Tool，并绑定给当前选中的员工。同步前先确认左侧当前员工是否正确。
- 未同步的 MCP Server 不会出现在公共工具广场，也不会出现在技能广场。

## 6. 日志与排查

```powershell
# 最近 100 行应用日志
Get-Content .dev\logs\app.log -Tail 100

# 持续查看应用日志
Get-Content .dev\logs\app.log -Tail 100 -Wait

# 启动器错误
Get-Content .dev\logs\supervisor.err.log -Tail 100

# 健康检查
curl.exe http://127.0.0.1:5173/api/health
```

- 修改前端后没有变化：执行 `dev_down.ps1`，再执行 `dev_up.ps1 --detach` 重新构建和启动。
- 本地 MCP 发现失败：检查 Command、Args、工作目录和 `.dev\logs\app.log`；`tools/list` 不依赖 OpenET Token。
- OpenET 调用提示 `AUTH_MISSING`：确认 `backend\.env` 中已配置 `OPENET_API_TOKEN`。
- `/docs` 提供 Swagger API 文档，`/api/health` 用于检查服务健康状态。
